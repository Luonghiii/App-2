import React, { FC } from 'react';

const Header: FC = () => (
    // The header content has been removed to de-brand the page, 
    // but some padding is kept to maintain the layout.
    <header className="pt-10" />
);

export default Header;